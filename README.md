# Todo-Web-Application with Component

This repository provides a skeleton, where a database and a pedestal application
should be composed with Component. Add the missing code to `system.clj` to get
it up and running!